import { connect } from "react-redux";

function Post() {
    return(
        <h1>Post page</h1>
    )
}

function mapState() {
    return
}

function mapDispatch() {
    return
}

export default connect(mapState, mapDispatch) (Post);