

function useEvent() {
    function addEvent(refArr, eventArr) {
        for (let i = 0; i < refArr.length; i++) {
            for (let j = 0; j < eventArr.length; j++) {
                
            }
        }
    }
    
    return addEvent;
}

export default useEvent;